<view class="tab-bar">
  <view
    class="tab-item {{selected === index ? 'is-active' : ''}}"
    wx:for="{{list}}"
    wx:key="pagePath"
    data-path="{{item.pagePath}}"
    data-index="{{index}}"
    bindtap="onSwitch"
  >
    <text class="tab-text">{{item.text}}</text>
  </view>
</view>
